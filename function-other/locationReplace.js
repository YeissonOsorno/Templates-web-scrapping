
let locations ={
    phoenix:"Phoenix, Arizona, US"
}

filter('phoenix');

function filter(location){
    const lci = Object.keys(locations);
    const lcf = Object.values(locations);
    for(const value of lci){
        if(location.)
    }
    console.log(lci + lcf)
}